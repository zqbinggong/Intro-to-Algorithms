
public class ArraySorts {
	int i,j,q;
	int key;
	
	
	public int[] InsertSort(int[] arr, int p, int q){
		
		for(i = 1; i < arr.length; i++){
			key = arr[i];
			j = i - 1;
			while(j>0 && arr[j] > key)
				arr[j+1] = arr[j++];
			arr[j+1] = key;
		}
		
		return arr;
	}

}
